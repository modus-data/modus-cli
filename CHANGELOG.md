# Changelog

## [Unreleased]

Initial Phase 1 build: auth (`login`/`logout`/`whoami`), scopes, workflows,
context, connections, usage, tools, and chat commands. See the package README
for scope and `tests/contract/operation-coverage.ts` for exact operation coverage.

- **`tools list`** — catalog of tool/integration surfaces a scope can select in its toolset.

Not yet published to npm — no tagged release yet.
