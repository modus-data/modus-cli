# Changelog

## [Unreleased]

Initial Phase 1 build: auth (`login`/`logout`/`whoami`), scopes, workflows,
context, connections, usage, and chat commands. See the package README for
scope and `tests/contract/operation-coverage.ts` for exact operation coverage.

Not yet published to npm — no tagged release yet.
